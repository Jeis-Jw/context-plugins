{
  "name": "context-core",
  "version": "0.4.1",
  "description": "Proactive scoped recall and approval-gated context capture with coordinated writes.",
  "author": {
    "name": "Jinwuk-Lee",
    "url": "https://github.com/Jeis-Jw"
  },
  "skills": "./skills/",
  "interface": {
    "displayName": "Context Core",
    "shortDescription": "Recall prior context and suggest durable capture safely.",
    "longDescription": "Installs a managed conversational workflow for scoped recall, actual-body comparison, conflict notification, and approval-gated capture; stores handoffs and evidence as Markdown and remains the only physical writer for context-common/v2 owners.",
    "developerName": "Jinwuk-Lee",
    "category": "Productivity",
    "capabilities": ["Read", "Write", "Validation"],
    "defaultPrompt": [
      "On each user turn, audit only the new semantic delta in the same response pass; when there is no durable signal, make no extra model or tool call and show no audit status. Keep only a compact session-local ephemeral ledger.",
      "Use $context-core:init only when the user asks to initialize the repository context root; that explicit call also installs or updates the active host's managed policy block.",
      "When a signal may change judgment, use $context-core:context metadata-first, read only selected actual Current bodies, and reuse their id and SHA only while the body remains in session context and anchors stay valid; report conflicts first, then offer mature grouped capture after the primary answer and require exact approval."
    ]
  }
}
