{
  "schema": "context-common-acceptance-corpus/v1",
  "cases": {
    "init_idempotent": {"input": "one explicit core init call, second init", "expected": ["doctor_ready", "second_diff_zero", "human_description_preserved", "host_policy_installed"]},
    "decision_core_missing": {"input": "decision init without exact core", "expected": ["core_missing", "manual_exact_provider_action", "write_zero"]},
    "natural_filename": {"input": "capture title without filename", "expected": ["no_type_prefix", "no_timestamp", "uuid_v4_context_id"]},
    "path_collision": {"input": "NFKC+casefold-equivalent basename", "expected": ["path_exists", "no_overwrite", "no_suffix"]},
    "rename_identity": {"input": "rename exact artifact id", "expected": ["id_preserved", "relations_preserved", "index_path_only_changed"]},
    "reserved_path": {"input": "root escape or artifact *.index.md", "expected": ["rejected", "write_zero"]},
    "index_determinism": {"input": "two rebuilds over identical documents", "expected": ["byte_identical", "self_entry_zero"]},
    "stage1_io": {"input": "healthy root and area indexes", "expected": ["artifact_open_zero", "artifact_list_zero", "artifact_stat_zero"]},
    "index_fallback": {"input": "broken area index or selected missing link", "expected": ["normal_area_fallback_warning", "strict_index_exit_6"]},
    "output_limit": {"input": "Stage 1 results above byte budget", "expected": ["complete_items_only", "truncated_true", "omitted_exact"]},
    "named_snapshots": {"input": "two named SNAP saves and one exact-id update", "expected": ["independent_ids", "save_collision_fails", "target_only_updated"]},
    "snapshot_update_merge": {"input": "full update then partial merge", "expected": ["full_requires_all_sections", "merge_preserves_omitted", "created_at_preserved"]},
    "snapshot_discard": {"input": "discard exact SNAP", "expected": ["file_removed", "index_removed", "retired_zero"]},
    "observation_invalidate": {"input": "invalidate exact OBS with reason", "expected": ["history_move", "reason_and_time_projected"]},
    "observation_supersede": {"input": "successor claim and same-claim lifecycle attestation", "expected": ["single_plan", "reciprocal_edges", "indexes_consistent"]},
    "repeated_supersede": {"input": "three OBS generations sharing natural filename", "expected": ["history_collision_zero", "id12_history_names"]},
    "decision_like_fallback": {"input": "decision-like candidate without decision owner", "expected": ["one_labeled_observation", "decision_authority_zero"]},
    "owner_installed": {"input": "same candidate with decision owner available", "expected": ["one_decision", "observation_fallback_suppressed"]},
    "explicit_unavailable_type": {"input": "explicit decision with unavailable owner", "expected": ["owner_unavailable", "silent_downgrade_zero"]},
    "explicit_invalid_type": {"input": "fact explicitly requested as decision", "expected": ["owner_decline", "authority_elevation_zero"]},
    "independent_fact_choice": {"input": "independent fact and choice claim keys", "expected": ["observation_and_decision_allowed"]},
    "owner_call_contract": {"input": "host-collected owner results", "expected": ["router_process_invocation_zero", "provided_results_only"]},
    "approval_preview": {"input": "complete owner result", "expected": ["sections_path_lifecycle_plan_digest_visible", "post_approval_generation_zero"]},
    "approval_digest": {"input": "tampered material, hidden operation, wrong digest, autonomous apply", "expected": ["all_fail_closed", "write_zero"]},
    "decision_sections": {"input": "missing or placeholder decision core section", "expected": ["rejected"]},
    "duplicate_slot": {"input": "second current decision in physical or virtual slot", "expected": ["decision_slot_conflict"]},
    "scope_overlap": {"input": "ancestor or descendant current decisions", "expected": ["all_conflicts_visible", "ordered_receipt_required", "ack_required"]},
    "decision_supersede": {"input": "supersede exact current decision", "expected": ["old_history", "new_current", "same_slot", "reciprocal_edges"]},
    "decision_withdraw": {"input": "withdraw exact current decision", "expected": ["history_without_successor", "excluded_from_current_recall"]},
    "revisit_due": {"input": "due revisit condition", "expected": ["warning_or_proposal_only", "write_zero"]},
    "evidence_observation_to_decision": {"input": "decision informed by evidence observation", "expected": ["observation_stays_active", "informed_by_edge"]},
    "fallback_observation_to_decision": {"input": "fallback OBS actual-claim and artifact-identity import", "expected": ["claim_and_same_claim_attested", "single_coordinator_plan", "observation_superseded"]},
    "addon_init": {"input": "decision descriptor and hashed complete seed", "expected": ["decision_direct_root_write_zero", "area_register_index_rebuild"]},
    "policy_install": {"input": "managed block preview for root policy", "expected": ["outside_bytes_preserved", "allowlist_and_digest_verified"]},
    "frontmatter_grammar": {"input": "JSON-compatible YAML valid and invalid corpus", "expected": ["canonical_roundtrip", "unknown_additive_preserved", "duplicate_and_unsupported_fail"]},
    "scope_key_normalization": {"input": "case, slash, space and underscore variants", "expected": ["canonical_slot", "segment_ancestor_only"]},
    "parallel_capture": {"input": "two approved bundles applied concurrently", "expected": ["duplicate_id_zero", "lost_index_entry_zero"]},
    "crash_resume": {"input": "changed move start, prepared and final states", "expected": ["same_bundle_converges", "document_authority_preserved", "repair_succeeds"]},
    "refresh_diagnostics": {"input": "out-of-band add, rename and frontmatter edit", "expected": ["all_drift_detected", "index_drift_warns", "document_not_overwritten"]},
    "obsidian_graph": {"input": "repository root opened as vault", "expected": ["root_to_area_to_artifact_hubs"]},
    "core_dependency_matrix": {"input": "wrong source, disabled and incompatible exact core", "expected": ["distinct_error_codes", "automatic_mutation_zero"]},
    "repository_absent": {"input": "exact core ready with absent context root", "expected": ["core_uninitialized", "installed_core_bootstrap", "decision_ready_same_call"]},
    "decision_init_bootstrap": {"input": "decision init with exact compatible installed core and absent repository", "expected": ["core_ready", "decision_ready", "retry_noop", "sole_writer"]},
    "bootstrap_phase_retry": {"input": "core phase succeeds before area phase failure", "expected": ["structured_phases", "retry_converges", "user_content_preserved"]},
    "distribution_manifests": {"input": "Claude, Codex and marketplace manifests", "expected": ["dependency_fields_zero", "implicit_install_policy_zero"]},
    "pre_decision_semantic_check": {"input": "paraphrased candidate and related Current DEC", "expected": ["actual_core_sections", "five_relation_contract", "write_zero"]},
    "removed_claim_identity_surrogates": {"input": "legacy claim identity field on artifact or capture candidate", "expected": ["artifact_warning", "authorized_lazy_clean", "candidate_rejected"]}
  }
}
